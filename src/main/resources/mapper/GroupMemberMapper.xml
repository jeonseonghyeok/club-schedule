<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moyora.clubschedule.mapper.GroupMemberMapper">

    <insert id="insertGroupMember" parameterType="com.moyora.clubschedule.vo.GroupMemberVo">
        INSERT INTO group_member (group_id, user_key, role, status, joined_at)
        VALUES (#{groupId}, #{userKey}, #{role}, #{status}, NOW())
    </insert>

    <select id="countActiveMembers" resultType="int" parameterType="long">
        SELECT COUNT(*) FROM group_member WHERE group_id = #{groupId} AND status = 'ACTIVE'
    </select>

    <select id="getGroupCapacity" resultType="int" parameterType="long">
        SELECT capacity FROM `group` WHERE group_id = #{groupId}
    </select>

    <select id="countByGroupAndUser" resultType="int" parameterType="map">
        SELECT COUNT(*) FROM group_member WHERE group_id = #{groupId} AND user_key = #{userKey} AND status = 'ACTIVE'
    </select>

</mapper>