<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moyora.clubschedule.mapper.ScheduleAttendanceCheckHistoryMapper">

    <insert id="insertHistory" parameterType="com.moyora.clubschedule.vo.ScheduleAttendanceCheckHistoryVo"
            useGeneratedKeys="true" keyProperty="historyId">
        INSERT INTO schedule_attendance_check_history (
            attendance_id, schedule_id, user_key,
            previous_actual_status, new_actual_status,
            changed_by, change_reason
        ) VALUES (
            #{attendanceId}, #{scheduleId}, #{userKey},
            #{previousActualStatus}, #{newActualStatus},
            #{changedBy}, #{changeReason}
        )
    </insert>

</mapper>
